---
- name: ufw
  hosts: localhost
  tasks:
  - name: Install the latest version of ufw package
    package:
      name: ufw
      state: latest
  - name: Ensure the ufw package is running
    service:
      name: ufw
      state: started
